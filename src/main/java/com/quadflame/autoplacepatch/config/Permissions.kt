package com.quadflame.autoplacepatch.config

class Permissions {

    companion object {
        const val RELOAD = "autoplacepatch.reload"
        const val BYPASS = "autoplacepatch.bypass"
        const val ALERTS = "autoplacepatch.alerts"
    }
}